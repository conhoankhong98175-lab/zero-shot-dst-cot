# Prompt Templates — Export Index

Generated: 2026-05-14 16:43

Each subdirectory contains the rendered prompt for one experimental variant.

Files are generated by `src/export_prompts.py` and reflect the **current**
state of `src/prompt_builder.py`. Re-run the script after any prompt edit.


## Variants

| Variant | Style | Steps included | Description |
|---|---|---|---|
| `standard` | No CoT | — | Standard Prompting (no CoT) |
| `cot_basic` | Free CoT | — | CoT-Basic (free-form 'think step by step') |
| `cot_full` | Structured CoT | S1 + S2 + S3 + S4 | CoT-Full (all 4 steps) |
| `ab_no_s1` | Structured CoT | S2 + S3 + S4 | Ablation: drop Step 1 (Domain Activation) |
| `ab_no_s2` | Structured CoT | S1 + S3 + S4 | Ablation: drop Step 2 (Explicit Search) |
| `ab_no_s3` | Structured CoT | S1 + S2 + S4 | Ablation: drop Step 3 (Implicit Inference) |
| `ab_no_s4` | Structured CoT | S1 + S2 + S3 | Ablation: drop Step 4 (None Decision) — expect ↑ hallucination |
| `fn_style` | Schema-only (no CoT) | — | Function-Calling Style (rich schema, no reasoning) |

## File Structure

```
prompts/
├── README.md                    ← this file
├── standard/
│   ├── system_prompt.txt        ← full system prompt
│   └── user_message.txt         ← example user turn
├── cot_basic/
│   ├── system_prompt.txt        ← full system prompt
│   └── user_message.txt         ← example user turn
├── cot_full/
│   ├── system_prompt.txt        ← full system prompt
│   └── user_message.txt         ← example user turn
├── ab_no_s1/
│   ├── system_prompt.txt        ← full system prompt
│   └── user_message.txt         ← example user turn
├── ab_no_s2/
│   ├── system_prompt.txt        ← full system prompt
│   └── user_message.txt         ← example user turn
├── ab_no_s3/
│   ├── system_prompt.txt        ← full system prompt
│   └── user_message.txt         ← example user turn
├── ab_no_s4/
│   ├── system_prompt.txt        ← full system prompt
│   └── user_message.txt         ← example user turn
├── fn_style/
│   ├── system_prompt.txt        ← full system prompt
│   └── user_message.txt         ← example user turn
```

## Demo Dialogue Used for `user_message.txt`

```
[turn 1] User: I'm looking for a cheap hotel in the east part of town.
         System: I found a few options. Do you have any other requirements?
[turn 2] User: Yes, it needs free parking and free wifi. I'll be staying for 3 nights.
```

Gold belief state:
```json
{"hotel-pricerange": "cheap", "hotel-area": "east",
 "hotel-parking": "yes", "hotel-internet": "yes", "hotel-stay": "3"}
```
